If you enter the webhook you don't need the token but if you enter the token the webhook is impossible
if you enter the webhook you don't need the channel ID but if you Enter The token you need the channel ID
